
/* "/musicquery/?q={music_id1,music_id2...}" */

export default function MusicDetailPage({ params }: { params: { id: string } }) {
  const musicId = params.id;

  

  return (
    <div>

    </div>
  );
}